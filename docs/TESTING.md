# 🧪 Тестирование

## Команды запуска

```bash
# Запуск тестов в режиме наблюдения (watch mode)
npm test

# Запуск тестов однократно без перезапуска
npm test -- --no-watch

# Запуск тестов в headless-браузере (для CI/CD)
npm test -- --no-watch --browsers=ChromeHeadless
```

## Технологии

- **Test Runner**: Karma 6.4.4
- **Testing Framework**: Jasmine
- **Моки**: Jasmine Spy Objects (`jasmine.createSpyObj`)
- **Async handling**: `fakeAsync` и `tick()` для тестирования асинхронных операций

## 📌 Тип тестов

**Unit-тесты (модульные тесты)** — тесты, которые проверяют отдельные единицы кода (функции, методы, классы) в изоляции от других частей системы.

| Характеристика | Описание |
|----------------|----------|
| **Изоляция** | Каждый компонент/сервис тестируется отдельно с использованием моков |
| **Моки (mocks)** | Зависимости заменяются заглушками (`jasmine.createSpyObj`) |
| **Скорость** | Выполняются быстро, так как нет реальных HTTP-запросов и DOM |
| **Scope** | Проверяют логику отдельных методов и функций |

**Что НЕ используется в проекте:**

| Тип теста | Описание | Почему не используется |
|-----------|----------|------------------------|
| **E2E (End-to-End)** | Тестирует всё приложение в браузере как реальный пользователь | Требует Protractor/Cypress, сложнее поддерживать |
| **Интеграционные** | Проверяет взаимодействие нескольких компонентов вместе | Unit-тесты с моками достаточны для текущей архитектуры |

## 📊 Статистика по тестам

| Файл тестов | Кол-во тестов | Что тестируется |
|-------------|---------------|-----------------|
| `app.component.spec.ts` | 3 | Корневой компонент |
| `article.service.spec.ts` | 17 | Сервис управления статьями |
| `annotation.service.spec.ts` | 17 | Сервис управления аннотациями |
| `article-list.component.spec.ts` | 10 | Компонент списка статей |
| `article-editor.component.spec.ts` | 21 | Компонент редактора статьи |
| **Итого** | **68** | |

---

## 🏠 AppComponent (3 теста)

| Тест | Описание |
|------|----------|
| `should create the app` | Проверка создания компонента |
| `should have the 'text-annotation-app' title` | Проверка заголовка приложения |
| `should render router-outlet` | Проверка наличия router-outlet |

---

## 📦 ArticleService (17 тестов)

**getAll()**
- Возвращает Observable с массивом статей
- Возвращает пустой массив при отсутствии данных

**create()**
- Создаёт новую статью и возвращает её
- Сохраняет статью в localStorage
- Генерирует уникальные ID для разных статей

**getById()**
- Возвращает статью по ID
- Возвращает undefined для несуществующего ID

**update()**
- Обновляет существующую статью
- Возвращает null для несуществующей статьи
- Сохраняет изменения в localStorage

**delete()**
- Удаляет существующую статью
- Возвращает false для несуществующей статьи
- Обновляет localStorage после удаления

**loadFromStorage()**
- Загружает статьи из localStorage при инициализации
- Корректно обрабатывает невалидный JSON (graceful degradation)

---

## 🏷️ AnnotationService (17 тестов)

**getByArticleId()**
- Возвращает аннотации для конкретной статьи
- Возвращает пустой массив для статьи без аннотаций

**create()**
- Создаёт новую аннотацию и возвращает её
- Сохраняет аннотацию в localStorage
- Генерирует уникальные ID (с префиксом `ann-`)

**update()**
- Обновляет примечание аннотации
- Обновляет цвет аннотации
- Возвращает null для несуществующей аннотации
- Сохраняет изменения в localStorage

**delete()**
- Удаляет существующую аннотацию
- Возвращает false для несуществующей аннотации
- Обновляет localStorage после удаления

**deleteByArticleId()**
- Удаляет все аннотации для конкретной статьи
- Возвращает true даже при отсутствии аннотаций

**loadFromStorage()**
- Загружает аннотации из localStorage при инициализации
- Корректно обрабатывает невалидный JSON (graceful degradation)

---

## 📋 ArticleListComponent (10 тестов)

**ngOnInit()**
- Загружает статьи при инициализации

**createNewArticle()**
- Навигация на страницу создания статьи (`/articles/new`)

**editArticle()**
- Навигация на страницу редактирования (`/articles/:id/edit`)

**viewArticle()**
- Навигация на страницу просмотра (`/articles/:id`)

**deleteArticle()**
- Не удаляет при отмене подтверждения
- Удаляет при подтверждении пользователя
- Останавливает всплытие события (stopPropagation)

**formatDate()**
- Форматирует timestamp в локализованную строку
- Корректно обрабатывает разные временные метки

---

## ✏️ ArticleEditorComponent (21 тест)

**ngOnInit — Режим новой статьи**
- Инициализация режима новой статьи при отсутствии ID
- Инициализация режима при ID = "new"

**ngOnInit — Режим редактирования**
- Установка editMode=true при query-параметре edit="true"

**ngOnInit — Загрузка статьи**
- Загрузка статьи и аннотаций по ID
- Навигация на главную при ненайденной статье

**saveArticle — Новая статья**
- Создание новой статьи
- Блокировка сохранения при пустых полях

**saveArticle — Существующая статья**
- Обновление существующей статьи

**cancelEdit()**
- Навигация на главную для новой статьи
- Выход из режима редактирования для существующей

**enterEditMode()**
- Включение режима редактирования

**goBack()**
- Навигация на главную страницу

**createAnnotation()**
- Создание аннотации и закрытие модального окна
- Блокировка создания без статьи

**cancelAnnotation()**
- Закрытие модального окна аннотации

**hideTooltip()**
- Скрытие тултипа и сброс текущей аннотации

**formatDate()**
- Форматирование timestamp

**darkenColor()**
- Затемнение HEX-цвета
- Не опускается ниже 0 для значений цвета

**colors**
- 6 доступных цветов
- Жёлтый как первый цвет по умолчанию

---

## ⚠️ Примечание об ошибках в консоли

При запуске тестов вы можете увидеть сообщения об ошибках:

```
ERROR: 'Error parsing articles from localStorage', SyntaxError: Unexpected token 'i', "invalid json" is not valid JSON
```

**Это нормально!** Эти сообщения появляются из тестов `should handle invalid JSON in localStorage gracefully`, которые специально проверяют обработку повреждённых данных. Сервисы корректно восстанавливаются и возвращают пустый массив.
